///  draft Snap! plugin controller

// takes a string as namespace
function Plugin(id) {
	this.id = id;


}

Plugin.prototype.idAlreadyInstalled() {
	if 
}

Plugin.prototype.install = function() {
	if (id == "") {
		throw "Plugin misconfigured: no plugin name";
	} else if ()
	
}

